module.exports = function transform(arr) {
    if (Array.isArray(arr) === false) {
        throw new Error('Error!'); 
    }

    let array = [];
    if (arr == []) return array;

    return array;
};
